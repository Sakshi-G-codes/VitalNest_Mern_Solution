<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Enter Blood Donation Data</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <div class="header">
        <h2>Enter Blood Donation Data for Request ID: <%= request_id %></h2>
    </div>
    <hr>
    <form action="/submitBloodDonationData" method="post">
        <input type="hidden" name="request_id" value="<%= request_id %>">
        <label for="donor_aadhar">Donor Aadhar:</label><br>
        <input type="text" id="donor_aadhar" name="donor_aadhar" pattern="\d{12}" maxlength="12" minlength="12" required><br><br>
        <label for="blood_group">Blood Group:</label><br>
        <select id="blood_group" name="blood_group" required>
            <option value="A+">A+</option>
            <option value="A-">A-</option>
            <option value="B+">B+</option>
            <option value="B-">B-</option>
            <option value="AB+">AB+</option>
            <option value="AB-">AB-</option>
            <option value="O+">O+</option>
            <option value="O-">O-</option>
        </select><br><br>
        <label for="tested_status">Tested Status:</label><br>
        <select id="tested_status" name="tested_status" required>
            <option value="passed">Passed</option>
            <option value="failed">Failed</option>
            <option value="pending">Pending</option>
        </select><br><br>
        <button type="submit">Submit Donation Data</button>
    </form>
    <hr>
    <h3>Existing Donations for this Request</h3>
    <% if (donations && donations.length > 0) { %>
    <table border="1" cellpadding="10" cellspacing="0">
        <thead>
            <tr>
                <th>Donor Aadhar</th>
                <th>Blood Group</th>
                <th>Tested Status</th>
                <th>Donation Timestamp</th>
            </tr>
        </thead>
        <tbody>
            <% donations.forEach(donation => { %>
            <tr>
                <td><%= donation.donor_aadhar %></td>
                <td><%= donation.blood_group %></td>
                <td><%= donation.tested_status %></td>
                <td><%= donation.donation_timestamp %></td>
            </tr>
            <% }) %>
        </tbody>
    </table>
    <% } else { %>
    <p>No donations recorded yet.</p>
    <% } %>
</body>
</html>
