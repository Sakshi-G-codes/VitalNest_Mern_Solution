<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Blood Donation Data</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <div class="header">
        <h2>Blood Donation Data for Request ID: {{ request_id }}</h2>
    </div>
    <hr>
    {% if donations %}
    <table border="1" cellpadding="10" cellspacing="0">
        <thead>
            <tr>
                <th>Donor Aadhar</th>
                <th>Blood Group</th>
                <th>Tested Status</th>
                <th>Donation Timestamp</th>
            </tr>
        </thead>
        <tbody>
            {% for donation in donations %}
            <tr>
                <td>{{ donation[2] }}</td>
                <td>{{ donation[3] }}</td>
                <td>{{ donation[4] }}</td>
                <td>{{ donation[5] }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% else %}
    <p>No donation data available for this request.</p>
    {% endif %}
</body>
</html>
